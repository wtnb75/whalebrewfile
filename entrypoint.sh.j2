#! /bin/sh
set -f

cmd=$1; shift
case "${cmd}" in
  {{commands|join('|')}})
    exec {{prefix|default('')}}${cmd} $*
    ;;
  *)
    echo "Usage: $0 [{{commands|join('|')}}] ..."
    exit 1
    ;;
esac
